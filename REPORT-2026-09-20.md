# LAPORAN UJI DEVICE — 2026-09-20 (com.linuxbox / proot session)

## HASIL AKHIR: ✅ PROOT SESSION HIDUP (sessionAlive=true)

Agent: opencode (local), device: Infinix X6855, Android 13 (Transsion ROM),
kernel /data = f2fs, SELinux **Enforcing**. adb wireless: `10.141.58.141:45307`.
Worktree: `linuxbox-build` @ `400efe0`.

### Verifikasi shell (kirim via WS port 8770)
```
__PROBE_START__
/
uid=10507 gid=10507 groups=3003(aid_inet),9997(aid_everybody),20507,50507
Linux localhost 6.12.38-android16-5-gb575a0b6e647-ab14355190-4k #1 SMP PREEMPT Wed Oct 29 06:56:12 UTC 2025 aarch64 Linux
__PROBE_END__
localhost:/$
```
- `healthz` → `{"running":true,"port":8770,"sessionAlive":true,"resize":true,...}`.
- Akar masalah = loader proot (`$PREFIX/libexec/proot/loader` di data Termux,
  tak bisa di-traverse app) → commit `400efe0` bundel parallel ke jniLibs +
  `PROOT_LOADER` env **menyelesaikan** `execve("/bin/sh"): Permission denied`.

### Catatan untuk lanjutan
- **Guest user = `uid 10507` (bukan root)**: proot tanpa `-0` memakai UID app
  host. Untuk guest root tambahkan flag `-0` di `ProotSession.buildCommand()`.
- `resize:true` sudah aktif (kanal kontrol ctrl.sock).
- Rootfs alpine offline (`DistroCatalog` BUILTIN url="") berjalan baik.

## Ringkasan (konteks sebelumnya)
- Commits arena `484c202` (TMPDIR host-side + `--cwd=/`) dan `bdfbeb7`
  (targetSdk 28) **TERVERIFIKASI di device** dan menghilangkan blok pertama:
  warning `can't canonicalize` .../usr/tmp, `f2fs bug probe`, `can't chdir`
  TIDAK MUNCUL LAGI.
- Blocker `execve("/bin/sh"): Permission denied` tersisa → SOLVED oleh `400efe0`.

## Bukti per sub-blok

### 1. Domain & targetSdk (commit bdfbeb7 berhasil)
```
ps -AZ | grep linuxbox → u:r:untrusted_app_27:s0:c250,c257,c512,c768
dumpsys package com.linuxbox → minSdk=26 targetSdk=28
```
- ternary domain SELinux berubah ke `untrusted_app_27` (cara Termux).
- Kategori app: `c250,c257,c512,c768`; kategori Termux: `c216,c257,c512,c768`.

### 2. Exec dari filesDir (W^X) — OK
- `run-as` probe: `./files/toybox_probe echo WX-OK-filesDir` → **WX-OK-filesDir**
  (binary di-copy ke filesDir, label `app_data_file`, dieksekusi).
- AVC grant nyata untuk app lain di domain sama (com.termux.api,
  `untrusted_app_27`, kategori c216):
  `granted { execute } / { execute_no_trans } path=/data/data/com.termux/files/usr/bin/bash` (dan curl)
  → untrusted_app_27 secara nyata BOLEH exec app_data_file milik sendiri.

### 3. AVC untuk com.linuxbox — TIDAK ada deny execute
Satu-satunya AVC deny com.linuxbox (full logcat -b all):
```
avc: denied { search } comm="libptylauncher."/"libproot.so" name="tests"
     tcontext=u:object_r:shell_test_data_file:s0 tclass=dir
```
- `name="tests"` di `/data/local/tmp` → artefak `device-diag.sh` (push t.in /
  inner.sh), bukan jalan app. NOISE, bukan penyebab.
- **TIDAK ADA `denied { execute }`** untuk busybox/sh/ld-musl → error
  `Permission denied` dari proot BUKAN keputusan SELinux app_data_file.

### 4. realpath() di domain app (anomali "/" HANDOFF) — SUDAH NORMAL
Dengan targetSdk 28 (domain untrusted_app_27):
```
toybox realpath /data/data/com.linuxbox/files/rootfs-alpine/bin/sh
→ /system/bin/busybox   (resolve symlink HOST; valid)
realpath files/rootfs-alpine → files/rootfs-alpine (rc=0) — bukan "/"
```

### 5. Infrastruktur jalan
- jniLibs: `/data/app/~~PJsbQW5OvjW8DnhJ3iDeJw==/com.linuxbox-P2TSgRWQaBt6yxDXwceurQ==/lib/arm64`
  → `libproot.so libptylauncher.so libtalloc.so.2 libandroid-shmem.so`,
  semua `u:object_r:apk_data_file:s0`.
- Rootfs: `files/rootfs-alpine` lengkap (bin/ash, busybox 919304B
  `-rwx--x--x`, ld-musl 723480B `-rwx--x--x`, bin/sh -> /bin/busybox).
- Server: `{"running":true,"port":8770,"sessionAlive":false,...}`.

## Hipotesis kerja (urut kemungkinan)
1. Error dari **dalam mekanisme ptrace/translation proot** (bukan sepolicy).
   Pola berubah: build lama → `execve("/bin/sh"): No such file or directory`,
   sekarang → `Permission denied` — keduanya dari *host-side* proot resolution
   yang berjalan salah, padahal guest path & file-nya nyata (bin/sh symlink ke
   busybox ada). Perlu melihat `-v 9` trace (lihat langkah 1 di bawah).
2. `PROOT_TMP_DIR` + varian C (TMPDIR host-side + --cwd=/) belum diuji
   terpisah — di-APP sudah jalan, tapi untuk mempersempit perlu dijalankan
   manual di luar pty (langkah 1).

## Rekomendasi langkah berikutnya (untuk arena)
1. **Betulkan dua bug kecil di `scripts/device-diag.sh`** lalu jalankan penuh:
   - `sh_adb() { "$ADB" shell ...; }` — `"$ADB"` quoted membuat `adb -s <dev>`
     jadi satu kata → `command not found`. Ganti jadi `$ADB shell ...`
     (tanpa quote variabel, atau pakai array).
   - deteksi NATIVE dari `dumpsys ... | grep nativeLibraryDir` sering kosong
     (yang ada `legacyNativeLibraryDir`). Hardcode `$BASE/lib/arm64`.
   Dengan itu Section 5 (varian A/B/C) dan Section 8 (`-v 9`) jalan — ini
   langsung memperlihatkan di extra APA proot men-deny.
2. **Uji minimal di dalam app domain** (bukan run-as): jalankan
   `libptylauncher.so` dengan argumen eksekusi langsung `.../bin/busybox echo hi`
   via server — bandingkan errno exec langsung vs lewat proot. Kalau exec
   langsung OK tapi lewat proot deny → lebih kuat ke mekanisme ptrace proot.
3. Kalau proot memang tidak mau jalan di ROM ini: termux `proot` biner yang sama
   DICOBA langsung di Termux (bukan app) → sangat mungkin WORKS (Termux sendiri
   proot-distro jalan). Verifikasi itu untuk memastikan binary OK sebelum ganti
   pendekatan (mis. proses host menggunakan proot milik Termux via
   `run-as`/binder → tidak, app tidak bisa; atau siapkan fallback terminal
   langsung `busybox sh` tanpa chroot untuk UAT, lalu tangani proot terpisah).
4. Note hardware: semua uji UI WAJIB lewat `monkey -p com.linuxbox` dulu;
   Termux sering di foreground, `input tap` buta bisa kena Termux.

## File & commit terkait
- `484c202` TMPDIR host-side + `--cwd=/` (verified).
- `bdfbeb7` targetSdk 28 → untrusted_app_27 (verified).
- `scripts/device-diag.sh` — butuh 2 fix kecil di atas.
- WIP lokal di worktree: tidak ada (clean at bdfbeb7).